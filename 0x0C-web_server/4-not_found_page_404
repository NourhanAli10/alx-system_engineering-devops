#!/usr/bin/env bash

# Update the package list to ensure the latest versions are available
sudo apt-get update

# Install Nginx web server if not already installed
sudo apt-get install -y nginx

# Create a backup of the default Nginx configuration
sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/default.bak

# Configure Nginx to use a custom 404 page with the string "Ceci n'est pas une page"
echo "
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    error_page 404 /404.html;
    location = /404.html {
        internal;
        root /usr/share/nginx/html;
        return 404 '<html><head><title>404 Not Found</title></head><body><h1>404 Not Found</h1><p>Ceci n\'est pas une page</p></body></html>';
    }

    # Additional server configuration...
}" | sudo tee /etc/nginx/sites-available/default > /dev/null

# Test the Nginx configuration
sudo nginx -t

# Reload Nginx to apply the changes
sudo systemctl reload nginx

# Output a message indicating successful configuration
echo "Nginx has been configured with a custom 404 page containing the string 'Ceci n'est pas une page'."
